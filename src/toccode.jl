function to_c(mf::TriangularMF)
    # replace this with actual body
    """
    The parameters of the membership function are
    a = $(mf.a)
    b = $(mf.b)
    c = $(mf.c)
    """
end

function to_c(mf::GaussianMF)
    # write code here
end

function to_c(mf::TrapezoidalMF)
    # write code here
end
